<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>HYBE Verification</title>
  <style>
    /* Base */
    body {
      margin:0;
      padding:0;
      background:#f4f4f9;
      font-family: Arial, Helvetica, sans-serif;
      color:#111111;
    }
    .container {
      max-width:600px;
      margin:20px auto;
      background:#ffffff;
      border:1px solid #e9e9e9;
      border-radius:12px;
      overflow:hidden;
    }
    /* Header */
    .header {
      text-align:center;
      padding:28px 20px;
      border-bottom:1px solid #e9e9e9;
    }
    .header img {
      max-width:100%;
      height:auto;
      width:160px; /* ensures nice size on desktop but shrinks on mobile */
    }
    /* Content */
    .content {
      padding:36px 28px;
      text-align:center;
    }
    .content h1 {
      font-size:22px;
      margin:0 0 10px;
      color:#111111;
    }
    .content h2 {
      font-size:18px;
      margin:0 0 18px;
      color:#222222;
      font-weight:normal;
    }
    .content p {
      font-size:15px;
      line-height:1.6;
      color:#444444;
      margin-bottom:24px;
    }
    /* OTP Box */
    .otp {
      display:inline-block;
      padding:16px 32px;
      background:#f7f7f7;
      border:1px solid #cccccc;
      border-radius:8px;
      font-size:32px;
      font-weight:bold;
      color:#111111;
      letter-spacing:4px;
      margin-bottom:26px;
      /* Subtle animation */
      animation:fadepulse 2.5s infinite;
    }
    @keyframes fadepulse {
      0%   { opacity:1; box-shadow:0 0 0 0 rgba(0,0,0,0.05); }
      50%  { opacity:0.95; box-shadow:0 0 10px 4px rgba(0,0,0,0.08); }
      100% { opacity:1; box-shadow:0 0 0 0 rgba(0,0,0,0.05); }
    }
    .instructions {
      font-size:13px;
      color:#666666;
    }
    /* Footer */
    .footer {
      background:#fafafa;
      border-top:1px solid #e9e9e9;
      padding:18px 22px;
      text-align:center;
      font-size:12px;
      color:#888888;
    }
    .footer a {
      color:#111111;
      text-decoration:none;
    }
    /* Dark mode */
    @media (prefers-color-scheme: dark) {
      body { background:#111111; color:#eeeeee; }
      .container { background:#1a1a1a; border-color:#333333; }
      .header { border-bottom-color:#333333; }
      .content h1, .content h2, .content p, .otp, .footer a { color:#f5f5f5; }
      .otp { background:#2a2a2a; border-color:#444444; }
      .footer { background:#141414; border-top-color:#333333; color:#aaaaaa; }
    }
  </style>
</head>
<body>
  <!-- Preheader (hidden in email body, shown in inbox preview) -->
  <div style="display:none; max-height:0; overflow:hidden; opacity:0; font-size:1px; line-height:1px; mso-hide:all;">
    Your HYBE verification code is inside. Expires in {{expirationMinutes}} minutes.
  </div>

  <div class="container">
    <!-- HEADER -->
    <div class="header">
      <img src="https://freeimghost.net/images/2025/08/29/HYBElogo.png" alt="HYBE Logo">
    </div>
    
    <!-- CONTENT -->
    <div class="content">
      <h1>Email Verification</h1>
      <h2>Your One-Time Password</h2>
      <p>Please use the following code to verify your email address for your HYBE booking request. This code is valid for <strong>{{expirationMinutes}}</strong> minutes.</p>
      <div class="otp">{{OTP}}</div>
      <p class="instructions">If you did not request this code, please ignore this email or contact our support team.</p>
    </div>
    
    <!-- FOOTER -->
    <div class="footer">
      <p>© HYBE. All Rights Reserved.</p>
      <p>
        <a href="https://hybecorp.com/eng/cookie">Cookie Policy</a> •
        <a href="https://hybecorp.com/kor/main">Privacy</a> •
        <a href="https://www.redwhistle.org/report/reportCheck.asp?organ=8230&amp;leng=EN">Whistleblowing</a>
      </p>
      <p>For support: <a href="mailto:hybe.corp@zohomail.com">bookings@hybe</a></p>
    </div>
  </div>
</body>
</html>
