# Environment Configuration Template
# Copy this file to .env and update the values for your environment

# === NODE.JS CONFIGURATION ===
NODE_ENV=production
NODE_VERSION=20.11.0

# === DATABASE CONFIGURATION ===
# Required: PostgreSQL connection string
# Format: postgresql://username:password@host:port/database?sslmode=require
# Example: postgresql://user:pass@hostname:5432/dbname?sslmode=require
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# === CACHE CONFIGURATION ===
# Optional: SQLite cache file path (defaults to server/db/cache.db)
SQLITE_DB_PATH=./cache.db

# === API CONFIGURATION ===
# Optional: Custom ping response message
PING_MESSAGE=pong

# === EMAIL CONFIGURATION (SMTP) ===
# Optional: If not provided, OTPs will be logged to console instead of emailed
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-app-password

# === RENDER.COM SPECIFIC ===
# These are automatically set by Render when using render.yaml
# DATABASE_URL=automatically_provided_by_render_database
# NODE_ENV=production

# === DEBUGGING ENDPOINTS ===
# Available health check endpoints for production monitoring:
# GET  /api/ping                          - Basic health check
# GET  /api/health/system                 - System health and metrics
# GET  /api/health/database               - Database connection status
# GET  /api/health/database/connection    - Database connection details
# GET  /api/health/database/schema        - Database schema validation
# POST /api/health/database/test          - Test database connection
# POST /api/health/database/init          - Initialize database schema

# === IMPORTANT PRODUCTION NOTES ===
# 1. Ensure DATABASE_URL points to a production PostgreSQL instance
# 2. Use SSL connections for production databases (sslmode=require)
# 3. Set NODE_ENV=production for optimized performance
# 4. Monitor the health check endpoints for system status
# 5. The app gracefully handles database unavailability with fallbacks
